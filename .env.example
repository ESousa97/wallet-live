# Conexão com o Postgres. O SQLx a usa tanto em tempo de COMPILAÇÃO (para checar
# as queries) quanto em runtime. Copie este arquivo para `.env` e ajuste se
# necessário — as credenciais abaixo batem com o docker-compose.yaml.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres
