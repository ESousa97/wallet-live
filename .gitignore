/target

# Variáveis de ambiente locais (ex.: DATABASE_URL). Veja .env.example.
.env

# Snapshots pendentes do insta (os aceitos, .snap, são versionados).
*.snap.new

# CAs específicos da máquina para o build Docker (ver docker/extra-ca/README.md).
docker/extra-ca/*.crt
docker/extra-ca/*.cer
